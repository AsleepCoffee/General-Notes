Bash has some built-in commands that provide basic func.

  fg, echo, set, while

Most other extrention programs that are kept in PATH get pulled from 

 /bin
 
 /usr/bin
 
 You can find a programs real location with "which"
